name: devworkspace_generic
services:
    terminal:
        build: .
        image: publishpress/dev-workspace-terminal:generic-4.2.0
        command: ["/bin/zsh"]
        stdin_open: true
        tty: true
        working_dir: "/project"
